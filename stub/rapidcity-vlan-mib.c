/*							-- DO NOT EDIT --
 * This file has been generated by smidump
 * version 0.2.17 for the scli package.
 *
 * Derived from RAPIDCITY-VLAN-MIB:
 *
 * $Id$
 */

#include "rapidcity-vlan-mib.h"

GSnmpEnum const rapidcity_vlan_mib_enums_rcVlanHighPriority[] = {
    { RAPIDCITY_VLAN_MIB_RCVLANHIGHPRIORITY_TRUE,	"true" },
    { RAPIDCITY_VLAN_MIB_RCVLANHIGHPRIORITY_FALSE,	"false" },
    { 0, NULL }
};

GSnmpEnum const rapidcity_vlan_mib_enums_rcVlanRoutingEnable[] = {
    { RAPIDCITY_VLAN_MIB_RCVLANROUTINGENABLE_TRUE,	"true" },
    { RAPIDCITY_VLAN_MIB_RCVLANROUTINGENABLE_FALSE,	"false" },
    { 0, NULL }
};

GSnmpEnum const rapidcity_vlan_mib_enums_rcVlanAction[] = {
    { RAPIDCITY_VLAN_MIB_RCVLANACTION_NONE,	"none" },
    { RAPIDCITY_VLAN_MIB_RCVLANACTION_FLUSHMACFDB,	"flushMacFdb" },
    { RAPIDCITY_VLAN_MIB_RCVLANACTION_FLUSHARP,	"flushArp" },
    { RAPIDCITY_VLAN_MIB_RCVLANACTION_FLUSHIP,	"flushIp" },
    { RAPIDCITY_VLAN_MIB_RCVLANACTION_FLUSHDYNMEMB,	"flushDynMemb" },
    { RAPIDCITY_VLAN_MIB_RCVLANACTION_ALL,	"all" },
    { 0, NULL }
};

GSnmpEnum const rapidcity_vlan_mib_enums_rcVlanResult[] = {
    { RAPIDCITY_VLAN_MIB_RCVLANRESULT_NONE,	"none" },
    { RAPIDCITY_VLAN_MIB_RCVLANRESULT_INPROGRESS,	"inProgress" },
    { RAPIDCITY_VLAN_MIB_RCVLANRESULT_SUCCESS,	"success" },
    { RAPIDCITY_VLAN_MIB_RCVLANRESULT_FAIL,	"fail" },
    { 0, NULL }
};

GSnmpEnum const rapidcity_vlan_mib_enums_rcVlanType[] = {
    { RAPIDCITY_VLAN_MIB_RCVLANTYPE_BYPORT,	"byPort" },
    { RAPIDCITY_VLAN_MIB_RCVLANTYPE_BYIPSUBNET,	"byIpSubnet" },
    { RAPIDCITY_VLAN_MIB_RCVLANTYPE_BYPROTOCOLID,	"byProtocolId" },
    { RAPIDCITY_VLAN_MIB_RCVLANTYPE_BYSRCMAC,	"bySrcMac" },
    { RAPIDCITY_VLAN_MIB_RCVLANTYPE_BYDSTMCAST,	"byDstMcast" },
    { 0, NULL }
};

GSnmpEnum const rapidcity_vlan_mib_enums_rcVlanProtocolId[] = {
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_NONE,	"none" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_IP,	"ip" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_IPX802DOT3,	"ipx802dot3" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_IPX802DOT2,	"ipx802dot2" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_IPXSNAP,	"ipxSnap" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_IPXETHERNET2,	"ipxEthernet2" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_APPLETALK,	"appleTalk" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_DECLAT,	"decLat" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_DECOTHER,	"decOther" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_SNA802DOT2,	"sna802dot2" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_SNAETHERNET2,	"snaEthernet2" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_NETBIOS,	"netBios" },
    { RAPIDCITY_VLAN_MIB_RCVLANPROTOCOLID_XNS,	"xns" },
    { 0, NULL }
};

GSnmpEnum const rapidcity_vlan_mib_enums_rcVlanRowStatus[] = {
    { RAPIDCITY_VLAN_MIB_RCVLANROWSTATUS_ACTIVE,	"active" },
    { RAPIDCITY_VLAN_MIB_RCVLANROWSTATUS_NOTINSERVICE,	"notInService" },
    { RAPIDCITY_VLAN_MIB_RCVLANROWSTATUS_NOTREADY,	"notReady" },
    { RAPIDCITY_VLAN_MIB_RCVLANROWSTATUS_CREATEANDGO,	"createAndGo" },
    { RAPIDCITY_VLAN_MIB_RCVLANROWSTATUS_CREATEANDWAIT,	"createAndWait" },
    { RAPIDCITY_VLAN_MIB_RCVLANROWSTATUS_DESTROY,	"destroy" },
    { 0, NULL }
};


typedef struct {
    guint32 const     subid;
    GSnmpVarBindType  type;
    gchar            *label;
} stls_stub_attr_t;

static void
add_attributes(GSnmpSession *s, GSList **vbl, guint32 *base, guint idx,
               stls_stub_attr_t *attributes)
{
    int i;

    for (i = 0; attributes[i].label; i++) {
        if (attributes[i].type != G_SNMP_COUNTER64 || s->version > G_SNMP_V1) {
            base[idx] = attributes[i].subid;
            g_snmp_vbl_add_null(vbl, base, idx + 1);
        }
    }
}

static int
lookup(GSnmpVarBind *vb, guint32 const *base, gsize const base_len,
	    stls_stub_attr_t *attributes, guint32 *idx)
{
    int i;

    if (vb->type == G_SNMP_ENDOFMIBVIEW
	|| (vb->type == G_SNMP_NOSUCHOBJECT)
	|| (vb->type == G_SNMP_NOSUCHINSTANCE)) {
	return -1;
    }
    
    if (memcmp(vb->id, base, base_len * sizeof(guint32)) != 0) {
	return -2;
    }

    for (i = 0; attributes[i].label; i++) {
	if (vb->id_len > base_len && vb->id[base_len] == attributes[i].subid) {
	    if (vb->type != attributes[i].type) {
		g_warning("type tag 0x%02x does not match 0x%02x (%s)",
			  vb->type, attributes[i].type, attributes[i].label);
		return -3;
	    }
	    *idx = attributes[i].subid;
	    return 0;
	}
    }
    
    return -4;
}

static stls_stub_attr_t _rcVlan[] = {
    { 1, G_SNMP_INTEGER32, "rcVlanNumVlans" },
    { 0, 0, NULL }
};

static stls_stub_attr_t _rcVlanEntry[] = {
    { 2, G_SNMP_OCTET_STRING, "rcVlanName" },
    { 3, G_SNMP_INTEGER32, "rcVlanColor" },
    { 4, G_SNMP_INTEGER32, "rcVlanHighPriority" },
    { 5, G_SNMP_INTEGER32, "rcVlanRoutingEnable" },
    { 6, G_SNMP_INTEGER32, "rcVlanIfIndex" },
    { 7, G_SNMP_INTEGER32, "rcVlanAction" },
    { 8, G_SNMP_INTEGER32, "rcVlanResult" },
    { 9, G_SNMP_INTEGER32, "rcVlanStgId" },
    { 10, G_SNMP_INTEGER32, "rcVlanType" },
    { 11, G_SNMP_OCTET_STRING, "rcVlanPortMembers" },
    { 12, G_SNMP_OCTET_STRING, "rcVlanPotentialMembers" },
    { 13, G_SNMP_OCTET_STRING, "rcVlanStaticMembers" },
    { 14, G_SNMP_OCTET_STRING, "rcVlanNotAllowToJoin" },
    { 15, G_SNMP_INTEGER32, "rcVlanProtocolId" },
    { 16, G_SNMP_IPADDRESS, "rcVlanSubnetAddr" },
    { 17, G_SNMP_IPADDRESS, "rcVlanSubnetMask" },
    { 18, G_SNMP_INTEGER32, "rcVlanAgingTime" },
    { 19, G_SNMP_OCTET_STRING, "rcVlanMacAddress" },
    { 20, G_SNMP_INTEGER32, "rcVlanRowStatus" },
    { 0, 0, NULL }
};


rapidcity_vlan_mib_rcVlan_t *
rapidcity_vlan_mib_new_rcVlan()
{
    rapidcity_vlan_mib_rcVlan_t *rcVlan;

    rcVlan = (rapidcity_vlan_mib_rcVlan_t *) g_malloc0(sizeof(rapidcity_vlan_mib_rcVlan_t) + sizeof(gpointer));
    return rcVlan;
}

static rapidcity_vlan_mib_rcVlan_t *
assign_rcVlan(GSList *vbl)
{
    GSList *elem;
    rapidcity_vlan_mib_rcVlan_t *rcVlan;
    guint32 idx;
    char *p;
    static guint32 const base[] = {1, 3, 6, 1, 4, 1, 2272, 1, 3};

    rcVlan = rapidcity_vlan_mib_new_rcVlan();
    if (! rcVlan) {
        return NULL;
    }

    p = (char *) rcVlan + sizeof(rapidcity_vlan_mib_rcVlan_t);
    * (GSList **) p = vbl;

    for (elem = vbl; elem; elem = g_slist_next(elem)) {
        GSnmpVarBind *vb = (GSnmpVarBind *) elem->data;

        if (lookup(vb, base, sizeof(base)/sizeof(guint32),
                   _rcVlan, &idx) < 0) continue;

        switch (idx) {
        case 1:
            rcVlan->rcVlanNumVlans = &(vb->syntax.i32[0]);
            break;
        };
    }

    return rcVlan;
}

int
rapidcity_vlan_mib_get_rcVlan(GSnmpSession *s, rapidcity_vlan_mib_rcVlan_t **rcVlan)
{
    GSList *in = NULL, *out = NULL;
    static guint32 base[] = {1, 3, 6, 1, 4, 1, 2272, 1, 3, 0};

    *rcVlan = NULL;

    add_attributes(s, &in, base, 9, _rcVlan);

    out = g_snmp_session_sync_getnext(s, in);
    g_snmp_vbl_free(in);
    if (! out) {
        return -2;
    }

    *rcVlan = assign_rcVlan(out);

    return 0;
}

void
rapidcity_vlan_mib_free_rcVlan(rapidcity_vlan_mib_rcVlan_t *rcVlan)
{
    GSList *vbl;
    char *p;

    if (rcVlan) {
        p = (char *) rcVlan + sizeof(rapidcity_vlan_mib_rcVlan_t);
        vbl = * (GSList **) p;
        g_snmp_vbl_free(vbl);
        g_free(rcVlan);
    }
}

rapidcity_vlan_mib_rcVlanEntry_t *
rapidcity_vlan_mib_new_rcVlanEntry()
{
    rapidcity_vlan_mib_rcVlanEntry_t *rcVlanEntry;

    rcVlanEntry = (rapidcity_vlan_mib_rcVlanEntry_t *) g_malloc0(sizeof(rapidcity_vlan_mib_rcVlanEntry_t) + sizeof(gpointer));
    return rcVlanEntry;
}

static int
unpack_rcVlanEntry(GSnmpVarBind *vb, rapidcity_vlan_mib_rcVlanEntry_t *rcVlanEntry)
{
    int idx = 12;

    if (vb->id_len < idx) return -1;
    rcVlanEntry->rcVlanId = vb->id[idx++];
    if (vb->id_len > idx) return -1;
    return 0;
}

static rapidcity_vlan_mib_rcVlanEntry_t *
assign_rcVlanEntry(GSList *vbl)
{
    GSList *elem;
    rapidcity_vlan_mib_rcVlanEntry_t *rcVlanEntry;
    guint32 idx;
    char *p;
    static guint32 const base[] = {1, 3, 6, 1, 4, 1, 2272, 1, 3, 2, 1};

    rcVlanEntry = rapidcity_vlan_mib_new_rcVlanEntry();
    if (! rcVlanEntry) {
        return NULL;
    }

    p = (char *) rcVlanEntry + sizeof(rapidcity_vlan_mib_rcVlanEntry_t);
    * (GSList **) p = vbl;

    if (unpack_rcVlanEntry((GSnmpVarBind *) vbl->data, rcVlanEntry) < 0) {
        g_warning("illegal rcVlanEntry instance identifier");
        g_free(rcVlanEntry);
        return NULL;
    }

    for (elem = vbl; elem; elem = g_slist_next(elem)) {
        GSnmpVarBind *vb = (GSnmpVarBind *) elem->data;

        if (lookup(vb, base, sizeof(base)/sizeof(guint32),
                   _rcVlanEntry, &idx) < 0) continue;

        switch (idx) {
        case 2:
            rcVlanEntry->_rcVlanNameLength = vb->syntax_len;
            rcVlanEntry->rcVlanName = vb->syntax.uc;
            break;
        case 3:
            rcVlanEntry->rcVlanColor = &(vb->syntax.i32[0]);
            break;
        case 4:
            rcVlanEntry->rcVlanHighPriority = &(vb->syntax.i32[0]);
            break;
        case 5:
            rcVlanEntry->rcVlanRoutingEnable = &(vb->syntax.i32[0]);
            break;
        case 6:
            rcVlanEntry->rcVlanIfIndex = &(vb->syntax.i32[0]);
            break;
        case 7:
            rcVlanEntry->rcVlanAction = &(vb->syntax.i32[0]);
            break;
        case 8:
            rcVlanEntry->rcVlanResult = &(vb->syntax.i32[0]);
            break;
        case 9:
            rcVlanEntry->rcVlanStgId = &(vb->syntax.i32[0]);
            break;
        case 10:
            rcVlanEntry->rcVlanType = &(vb->syntax.i32[0]);
            break;
        case 11:
            rcVlanEntry->rcVlanPortMembers = vb->syntax.uc;
            break;
        case 12:
            rcVlanEntry->rcVlanPotentialMembers = vb->syntax.uc;
            break;
        case 13:
            rcVlanEntry->rcVlanStaticMembers = vb->syntax.uc;
            break;
        case 14:
            rcVlanEntry->rcVlanNotAllowToJoin = vb->syntax.uc;
            break;
        case 15:
            rcVlanEntry->rcVlanProtocolId = &(vb->syntax.i32[0]);
            break;
        case 16:
            rcVlanEntry->rcVlanSubnetAddr = vb->syntax.uc;
            break;
        case 17:
            rcVlanEntry->rcVlanSubnetMask = vb->syntax.uc;
            break;
        case 18:
            rcVlanEntry->rcVlanAgingTime = &(vb->syntax.i32[0]);
            break;
        case 19:
            rcVlanEntry->rcVlanMacAddress = vb->syntax.uc;
            break;
        case 20:
            rcVlanEntry->rcVlanRowStatus = &(vb->syntax.i32[0]);
            break;
        };
    }

    return rcVlanEntry;
}

int
rapidcity_vlan_mib_get_rcVlanTable(GSnmpSession *s, rapidcity_vlan_mib_rcVlanEntry_t ***rcVlanEntry)
{
    GSList *in = NULL, *out = NULL;
    GSList *row;
    int i;
    static guint32 base[] = {1, 3, 6, 1, 4, 1, 2272, 1, 3, 2, 1, 0};

    *rcVlanEntry = NULL;

    add_attributes(s, &in, base, 11, _rcVlanEntry);

    out = gsnmp_gettable(s, in);
    /* g_snmp_vbl_free(in); */
    if (! out) {
        return -2;
    }

    *rcVlanEntry = (rapidcity_vlan_mib_rcVlanEntry_t **) g_malloc0((g_slist_length(out) + 1) * sizeof(rapidcity_vlan_mib_rcVlanEntry_t *));
    if (! *rcVlanEntry) {
        return -4;
    }

    for (row = out, i = 0; row; row = g_slist_next(row), i++) {
        (*rcVlanEntry)[i] = assign_rcVlanEntry(row->data);
    }

    return 0;
}

void
rapidcity_vlan_mib_free_rcVlanEntry(rapidcity_vlan_mib_rcVlanEntry_t *rcVlanEntry)
{
    GSList *vbl;
    char *p;

    if (rcVlanEntry) {
        p = (char *) rcVlanEntry + sizeof(rapidcity_vlan_mib_rcVlanEntry_t);
        vbl = * (GSList **) p;
        g_snmp_vbl_free(vbl);
        g_free(rcVlanEntry);
    }
}

void
rapidcity_vlan_mib_free_rcVlanTable(rapidcity_vlan_mib_rcVlanEntry_t **rcVlanEntry)
{
    int i;

    if (rcVlanEntry) {
        for (i = 0; rcVlanEntry[i]; i++) {
            rapidcity_vlan_mib_free_rcVlanEntry(rcVlanEntry[i]);
        }
        g_free(rcVlanEntry);
    }
}


