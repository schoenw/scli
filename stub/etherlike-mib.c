/*							-- DO NOT EDIT --
 * This file has been generated by smidump
 * version 0.2.14 for the stools package.
 *
 * Derived from EtherLike-MIB:
 *   The MIB module to describe generic objects for
 *   Ethernet-like network interfaces.
 *   
 *   The following reference is used throughout this
 *   MIB module:
 *   
 *   [IEEE 802.3 Std] refers to:
 *      IEEE Std 802.3, 1998 Edition: 'Information
 *      technology - Telecommunications and
 *      information exchange between systems -
 *      Local and metropolitan area networks -
 *      Specific requirements - Part 3: Carrier
 *      sense multiple access with collision
 *      detection (CSMA/CD) access method and
 *      physical layer specifications',
 *      September 1998.
 *   
 *   Of particular interest is Clause 30, '10Mb/s,
 *   100Mb/s and 1000Mb/s Management'.
 *
 * Revision 1999-08-24 04:00:
 *   Updated to include support for 1000 Mb/sec
 *   interfaces and full-duplex interfaces.
 *   This version published as RFC 2665.
 *
 * Revision 1998-06-03 21:50:
 *   Updated to include support for 100 Mb/sec
 *   interfaces.
 *   This version published as RFC 2358.
 *
 * Revision 1994-02-03 04:00:
 *   Initial version, published as RFC 1650.
 *
 * $Id$
 */

#include "etherlike-mib.h"

stls_enum_t const etherlike_mib_enums_dot3StatsDuplexStatus[] = {
    { ETHERLIKE_MIB_DOT3STATSDUPLEXSTATUS_UNKNOWN,	"unknown" },
    { ETHERLIKE_MIB_DOT3STATSDUPLEXSTATUS_HALFDUPLEX,	"halfDuplex" },
    { ETHERLIKE_MIB_DOT3STATSDUPLEXSTATUS_FULLDUPLEX,	"fullDuplex" },
    { 0, NULL }
};

stls_enum_t const etherlike_mib_enums_dot3PauseAdminMode[] = {
    { ETHERLIKE_MIB_DOT3PAUSEADMINMODE_DISABLED,	"disabled" },
    { ETHERLIKE_MIB_DOT3PAUSEADMINMODE_ENABLEDXMIT,	"enabledXmit" },
    { ETHERLIKE_MIB_DOT3PAUSEADMINMODE_ENABLEDRCV,	"enabledRcv" },
    { ETHERLIKE_MIB_DOT3PAUSEADMINMODE_ENABLEDXMITANDRCV,	"enabledXmitAndRcv" },
    { 0, NULL }
};

stls_enum_t const etherlike_mib_enums_dot3PauseOperMode[] = {
    { ETHERLIKE_MIB_DOT3PAUSEOPERMODE_DISABLED,	"disabled" },
    { ETHERLIKE_MIB_DOT3PAUSEOPERMODE_ENABLEDXMIT,	"enabledXmit" },
    { ETHERLIKE_MIB_DOT3PAUSEOPERMODE_ENABLEDRCV,	"enabledRcv" },
    { ETHERLIKE_MIB_DOT3PAUSEOPERMODE_ENABLEDXMITANDRCV,	"enabledXmitAndRcv" },
    { 0, NULL }
};


dot3StatsEntry_t *
etherlike_mib_new_dot3StatsEntry()
{
    dot3StatsEntry_t *dot3StatsEntry;

    dot3StatsEntry = (dot3StatsEntry_t *) g_malloc0(sizeof(dot3StatsEntry_t) + sizeof(gpointer));
    return dot3StatsEntry;
}

static int
unpack_dot3StatsEntry(GSnmpVarBind *vb, dot3StatsEntry_t *dot3StatsEntry)
{
    int idx = 11;

    if (vb->id_len < idx) return -1;
    dot3StatsEntry->dot3StatsIndex = vb->id[idx++];
    if (vb->id_len > idx) return -1;
    return 0;
}

static dot3StatsEntry_t *
assign_dot3StatsEntry(GSList *vbl)
{
    GSList *elem;
    dot3StatsEntry_t *dot3StatsEntry;
    char *p;
    static guint32 const base[] = {1, 3, 6, 1, 2, 1, 10, 7, 2, 1};

    dot3StatsEntry = etherlike_mib_new_dot3StatsEntry();
    if (! dot3StatsEntry) {
        return NULL;
    }

    p = (char *) dot3StatsEntry + sizeof(dot3StatsEntry_t);
    * (GSList **) p = vbl;

    if (unpack_dot3StatsEntry((GSnmpVarBind *) vbl->data, dot3StatsEntry) < 0) {
        g_warning("illegal dot3StatsEntry instance identifier");
        g_free(dot3StatsEntry);
        return NULL;
    }

    for (elem = vbl; elem; elem = g_slist_next(elem)) {
        GSnmpVarBind *vb = (GSnmpVarBind *) elem->data;
        if (vb->type == G_SNMP_ENDOFMIBVIEW
            || (vb->type == G_SNMP_NOSUCHOBJECT)
            || (vb->type == G_SNMP_NOSUCHINSTANCE)) {
            continue;
        }
        if (memcmp(vb->id, base, sizeof(base)) != 0) {
            continue;
        }
        if (vb->id_len > 11 && vb->id[10] == 2) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsAlignmentErrors = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsAlignmentErrors");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 3) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsFCSErrors = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsFCSErrors");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 4) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsSingleCollisionFrames = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsSingleCollisionFrames");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 5) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsMultipleCollisionFrames = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsMultipleCollisionFrames");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 6) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsSQETestErrors = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsSQETestErrors");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 7) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsDeferredTransmissions = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsDeferredTransmissions");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 8) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsLateCollisions = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsLateCollisions");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 9) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsExcessiveCollisions = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsExcessiveCollisions");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 10) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsInternalMacTransmitErrors = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsInternalMacTransmitErrors");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 11) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsCarrierSenseErrors = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsCarrierSenseErrors");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 13) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsFrameTooLongs = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsFrameTooLongs");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 16) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsInternalMacReceiveErrors = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsInternalMacReceiveErrors");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 17) {
            if (vb->type == G_SNMP_OBJECT_ID) {
                dot3StatsEntry->_dot3StatsEtherChipSetLength = vb->syntax_len / sizeof(guint32);
                dot3StatsEntry->dot3StatsEtherChipSet = vb->syntax.ui32;
            } else {
                g_warning("illegal type for dot3StatsEtherChipSet");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 18) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3StatsEntry->dot3StatsSymbolErrors = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3StatsSymbolErrors");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 19) {
            if (vb->type == G_SNMP_INTEGER32) {
                dot3StatsEntry->dot3StatsDuplexStatus = &(vb->syntax.i32[0]);
            } else {
                g_warning("illegal type for dot3StatsDuplexStatus");
            }
        }
    }

    return dot3StatsEntry;
}

int
etherlike_mib_get_dot3StatsTable(host_snmp *s, dot3StatsEntry_t ***dot3StatsEntry)
{
    GSList *in = NULL, *out = NULL;
    GSList *row;
    int i;
    static guint32 base[] = {1, 3, 6, 1, 2, 1, 10, 7, 2, 1, 0};

    *dot3StatsEntry = NULL;

    base[10] = 2; stls_vbl_add_null(&in, base, 11);
    base[10] = 3; stls_vbl_add_null(&in, base, 11);
    base[10] = 4; stls_vbl_add_null(&in, base, 11);
    base[10] = 5; stls_vbl_add_null(&in, base, 11);
    base[10] = 6; stls_vbl_add_null(&in, base, 11);
    base[10] = 7; stls_vbl_add_null(&in, base, 11);
    base[10] = 8; stls_vbl_add_null(&in, base, 11);
    base[10] = 9; stls_vbl_add_null(&in, base, 11);
    base[10] = 10; stls_vbl_add_null(&in, base, 11);
    base[10] = 11; stls_vbl_add_null(&in, base, 11);
    base[10] = 13; stls_vbl_add_null(&in, base, 11);
    base[10] = 16; stls_vbl_add_null(&in, base, 11);
    base[10] = 17; stls_vbl_add_null(&in, base, 11);
    base[10] = 18; stls_vbl_add_null(&in, base, 11);
    base[10] = 19; stls_vbl_add_null(&in, base, 11);

    out = stls_snmp_gettable(s, in);
    /* stls_vbl_free(in); */
    if (! out) {
        return -2;
    }

    *dot3StatsEntry = (dot3StatsEntry_t **) g_malloc0((g_slist_length(out) + 1) * sizeof(dot3StatsEntry_t *));
    if (! *dot3StatsEntry) {
        return -4;
    }

    for (row = out, i = 0; row; row = g_slist_next(row), i++) {
        (*dot3StatsEntry)[i] = assign_dot3StatsEntry(row->data);
    }

    return 0;
}

void
etherlike_mib_free_dot3StatsEntry(dot3StatsEntry_t *dot3StatsEntry)
{
    GSList *vbl;
    char *p;

    if (dot3StatsEntry) {
        p = (char *) dot3StatsEntry + sizeof(dot3StatsEntry_t);
        vbl = * (GSList **) p;
        stls_vbl_free(vbl);
        g_free(dot3StatsEntry);
    }
}

void
etherlike_mib_free_dot3StatsTable(dot3StatsEntry_t **dot3StatsEntry)
{
    int i;

    if (dot3StatsEntry) {
        for (i = 0; dot3StatsEntry[i]; i++) {
            etherlike_mib_free_dot3StatsEntry(dot3StatsEntry[i]);
        }
        g_free(dot3StatsEntry);
    }
}

dot3CollEntry_t *
etherlike_mib_new_dot3CollEntry()
{
    dot3CollEntry_t *dot3CollEntry;

    dot3CollEntry = (dot3CollEntry_t *) g_malloc0(sizeof(dot3CollEntry_t) + sizeof(gpointer));
    return dot3CollEntry;
}

static int
unpack_dot3CollEntry(GSnmpVarBind *vb, dot3CollEntry_t *dot3CollEntry)
{
    int idx = 11;

    if (vb->id_len < idx) return -1;
    dot3CollEntry->ifIndex = vb->id[idx++];
    if (vb->id_len < idx) return -1;
    dot3CollEntry->dot3CollCount = vb->id[idx++];
    if (vb->id_len > idx) return -1;
    return 0;
}

static dot3CollEntry_t *
assign_dot3CollEntry(GSList *vbl)
{
    GSList *elem;
    dot3CollEntry_t *dot3CollEntry;
    char *p;
    static guint32 const base[] = {1, 3, 6, 1, 2, 1, 10, 7, 5, 1};

    dot3CollEntry = etherlike_mib_new_dot3CollEntry();
    if (! dot3CollEntry) {
        return NULL;
    }

    p = (char *) dot3CollEntry + sizeof(dot3CollEntry_t);
    * (GSList **) p = vbl;

    if (unpack_dot3CollEntry((GSnmpVarBind *) vbl->data, dot3CollEntry) < 0) {
        g_warning("illegal dot3CollEntry instance identifier");
        g_free(dot3CollEntry);
        return NULL;
    }

    for (elem = vbl; elem; elem = g_slist_next(elem)) {
        GSnmpVarBind *vb = (GSnmpVarBind *) elem->data;
        if (vb->type == G_SNMP_ENDOFMIBVIEW
            || (vb->type == G_SNMP_NOSUCHOBJECT)
            || (vb->type == G_SNMP_NOSUCHINSTANCE)) {
            continue;
        }
        if (memcmp(vb->id, base, sizeof(base)) != 0) {
            continue;
        }
        if (vb->id_len > 11 && vb->id[10] == 3) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3CollEntry->dot3CollFrequencies = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3CollFrequencies");
            }
        }
    }

    return dot3CollEntry;
}

int
etherlike_mib_get_dot3CollTable(host_snmp *s, dot3CollEntry_t ***dot3CollEntry)
{
    GSList *in = NULL, *out = NULL;
    GSList *row;
    int i;
    static guint32 base[] = {1, 3, 6, 1, 2, 1, 10, 7, 5, 1, 0};

    *dot3CollEntry = NULL;

    base[10] = 3; stls_vbl_add_null(&in, base, 11);

    out = stls_snmp_gettable(s, in);
    /* stls_vbl_free(in); */
    if (! out) {
        return -2;
    }

    *dot3CollEntry = (dot3CollEntry_t **) g_malloc0((g_slist_length(out) + 1) * sizeof(dot3CollEntry_t *));
    if (! *dot3CollEntry) {
        return -4;
    }

    for (row = out, i = 0; row; row = g_slist_next(row), i++) {
        (*dot3CollEntry)[i] = assign_dot3CollEntry(row->data);
    }

    return 0;
}

void
etherlike_mib_free_dot3CollEntry(dot3CollEntry_t *dot3CollEntry)
{
    GSList *vbl;
    char *p;

    if (dot3CollEntry) {
        p = (char *) dot3CollEntry + sizeof(dot3CollEntry_t);
        vbl = * (GSList **) p;
        stls_vbl_free(vbl);
        g_free(dot3CollEntry);
    }
}

void
etherlike_mib_free_dot3CollTable(dot3CollEntry_t **dot3CollEntry)
{
    int i;

    if (dot3CollEntry) {
        for (i = 0; dot3CollEntry[i]; i++) {
            etherlike_mib_free_dot3CollEntry(dot3CollEntry[i]);
        }
        g_free(dot3CollEntry);
    }
}

dot3ControlEntry_t *
etherlike_mib_new_dot3ControlEntry()
{
    dot3ControlEntry_t *dot3ControlEntry;

    dot3ControlEntry = (dot3ControlEntry_t *) g_malloc0(sizeof(dot3ControlEntry_t) + sizeof(gpointer));
    return dot3ControlEntry;
}

static int
unpack_dot3ControlEntry(GSnmpVarBind *vb, dot3ControlEntry_t *dot3ControlEntry)
{
    int idx = 11;

    if (vb->id_len < idx) return -1;
    dot3ControlEntry->dot3StatsIndex = vb->id[idx++];
    if (vb->id_len > idx) return -1;
    return 0;
}

static dot3ControlEntry_t *
assign_dot3ControlEntry(GSList *vbl)
{
    GSList *elem;
    dot3ControlEntry_t *dot3ControlEntry;
    char *p;
    static guint32 const base[] = {1, 3, 6, 1, 2, 1, 10, 7, 9, 1};

    dot3ControlEntry = etherlike_mib_new_dot3ControlEntry();
    if (! dot3ControlEntry) {
        return NULL;
    }

    p = (char *) dot3ControlEntry + sizeof(dot3ControlEntry_t);
    * (GSList **) p = vbl;

    if (unpack_dot3ControlEntry((GSnmpVarBind *) vbl->data, dot3ControlEntry) < 0) {
        g_warning("illegal dot3ControlEntry instance identifier");
        g_free(dot3ControlEntry);
        return NULL;
    }

    for (elem = vbl; elem; elem = g_slist_next(elem)) {
        GSnmpVarBind *vb = (GSnmpVarBind *) elem->data;
        if (vb->type == G_SNMP_ENDOFMIBVIEW
            || (vb->type == G_SNMP_NOSUCHOBJECT)
            || (vb->type == G_SNMP_NOSUCHINSTANCE)) {
            continue;
        }
        if (memcmp(vb->id, base, sizeof(base)) != 0) {
            continue;
        }
        if (vb->id_len > 11 && vb->id[10] == 1) {
            if (vb->type == G_SNMP_OCTET_STRING) {
                dot3ControlEntry->_dot3ControlFunctionsSupportedLength = vb->syntax_len;
                dot3ControlEntry->dot3ControlFunctionsSupported = vb->syntax.uc;
            } else {
                g_warning("illegal type for dot3ControlFunctionsSupported");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 2) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3ControlEntry->dot3ControlInUnknownOpcodes = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3ControlInUnknownOpcodes");
            }
        }
    }

    return dot3ControlEntry;
}

int
etherlike_mib_get_dot3ControlTable(host_snmp *s, dot3ControlEntry_t ***dot3ControlEntry)
{
    GSList *in = NULL, *out = NULL;
    GSList *row;
    int i;
    static guint32 base[] = {1, 3, 6, 1, 2, 1, 10, 7, 9, 1, 0};

    *dot3ControlEntry = NULL;

    base[10] = 1; stls_vbl_add_null(&in, base, 11);
    base[10] = 2; stls_vbl_add_null(&in, base, 11);

    out = stls_snmp_gettable(s, in);
    /* stls_vbl_free(in); */
    if (! out) {
        return -2;
    }

    *dot3ControlEntry = (dot3ControlEntry_t **) g_malloc0((g_slist_length(out) + 1) * sizeof(dot3ControlEntry_t *));
    if (! *dot3ControlEntry) {
        return -4;
    }

    for (row = out, i = 0; row; row = g_slist_next(row), i++) {
        (*dot3ControlEntry)[i] = assign_dot3ControlEntry(row->data);
    }

    return 0;
}

void
etherlike_mib_free_dot3ControlEntry(dot3ControlEntry_t *dot3ControlEntry)
{
    GSList *vbl;
    char *p;

    if (dot3ControlEntry) {
        p = (char *) dot3ControlEntry + sizeof(dot3ControlEntry_t);
        vbl = * (GSList **) p;
        stls_vbl_free(vbl);
        g_free(dot3ControlEntry);
    }
}

void
etherlike_mib_free_dot3ControlTable(dot3ControlEntry_t **dot3ControlEntry)
{
    int i;

    if (dot3ControlEntry) {
        for (i = 0; dot3ControlEntry[i]; i++) {
            etherlike_mib_free_dot3ControlEntry(dot3ControlEntry[i]);
        }
        g_free(dot3ControlEntry);
    }
}

dot3PauseEntry_t *
etherlike_mib_new_dot3PauseEntry()
{
    dot3PauseEntry_t *dot3PauseEntry;

    dot3PauseEntry = (dot3PauseEntry_t *) g_malloc0(sizeof(dot3PauseEntry_t) + sizeof(gpointer));
    return dot3PauseEntry;
}

static int
unpack_dot3PauseEntry(GSnmpVarBind *vb, dot3PauseEntry_t *dot3PauseEntry)
{
    int idx = 11;

    if (vb->id_len < idx) return -1;
    dot3PauseEntry->dot3StatsIndex = vb->id[idx++];
    if (vb->id_len > idx) return -1;
    return 0;
}

static dot3PauseEntry_t *
assign_dot3PauseEntry(GSList *vbl)
{
    GSList *elem;
    dot3PauseEntry_t *dot3PauseEntry;
    char *p;
    static guint32 const base[] = {1, 3, 6, 1, 2, 1, 10, 7, 10, 1};

    dot3PauseEntry = etherlike_mib_new_dot3PauseEntry();
    if (! dot3PauseEntry) {
        return NULL;
    }

    p = (char *) dot3PauseEntry + sizeof(dot3PauseEntry_t);
    * (GSList **) p = vbl;

    if (unpack_dot3PauseEntry((GSnmpVarBind *) vbl->data, dot3PauseEntry) < 0) {
        g_warning("illegal dot3PauseEntry instance identifier");
        g_free(dot3PauseEntry);
        return NULL;
    }

    for (elem = vbl; elem; elem = g_slist_next(elem)) {
        GSnmpVarBind *vb = (GSnmpVarBind *) elem->data;
        if (vb->type == G_SNMP_ENDOFMIBVIEW
            || (vb->type == G_SNMP_NOSUCHOBJECT)
            || (vb->type == G_SNMP_NOSUCHINSTANCE)) {
            continue;
        }
        if (memcmp(vb->id, base, sizeof(base)) != 0) {
            continue;
        }
        if (vb->id_len > 11 && vb->id[10] == 1) {
            if (vb->type == G_SNMP_INTEGER32) {
                dot3PauseEntry->dot3PauseAdminMode = &(vb->syntax.i32[0]);
            } else {
                g_warning("illegal type for dot3PauseAdminMode");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 2) {
            if (vb->type == G_SNMP_INTEGER32) {
                dot3PauseEntry->dot3PauseOperMode = &(vb->syntax.i32[0]);
            } else {
                g_warning("illegal type for dot3PauseOperMode");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 3) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3PauseEntry->dot3InPauseFrames = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3InPauseFrames");
            }
        }
        if (vb->id_len > 11 && vb->id[10] == 4) {
            if (vb->type == G_SNMP_COUNTER32) {
                dot3PauseEntry->dot3OutPauseFrames = &(vb->syntax.ui32[0]);
            } else {
                g_warning("illegal type for dot3OutPauseFrames");
            }
        }
    }

    return dot3PauseEntry;
}

int
etherlike_mib_get_dot3PauseTable(host_snmp *s, dot3PauseEntry_t ***dot3PauseEntry)
{
    GSList *in = NULL, *out = NULL;
    GSList *row;
    int i;
    static guint32 base[] = {1, 3, 6, 1, 2, 1, 10, 7, 10, 1, 0};

    *dot3PauseEntry = NULL;

    base[10] = 1; stls_vbl_add_null(&in, base, 11);
    base[10] = 2; stls_vbl_add_null(&in, base, 11);
    base[10] = 3; stls_vbl_add_null(&in, base, 11);
    base[10] = 4; stls_vbl_add_null(&in, base, 11);

    out = stls_snmp_gettable(s, in);
    /* stls_vbl_free(in); */
    if (! out) {
        return -2;
    }

    *dot3PauseEntry = (dot3PauseEntry_t **) g_malloc0((g_slist_length(out) + 1) * sizeof(dot3PauseEntry_t *));
    if (! *dot3PauseEntry) {
        return -4;
    }

    for (row = out, i = 0; row; row = g_slist_next(row), i++) {
        (*dot3PauseEntry)[i] = assign_dot3PauseEntry(row->data);
    }

    return 0;
}

void
etherlike_mib_free_dot3PauseEntry(dot3PauseEntry_t *dot3PauseEntry)
{
    GSList *vbl;
    char *p;

    if (dot3PauseEntry) {
        p = (char *) dot3PauseEntry + sizeof(dot3PauseEntry_t);
        vbl = * (GSList **) p;
        stls_vbl_free(vbl);
        g_free(dot3PauseEntry);
    }
}

void
etherlike_mib_free_dot3PauseTable(dot3PauseEntry_t **dot3PauseEntry)
{
    int i;

    if (dot3PauseEntry) {
        for (i = 0; dot3PauseEntry[i]; i++) {
            etherlike_mib_free_dot3PauseEntry(dot3PauseEntry[i]);
        }
        g_free(dot3PauseEntry);
    }
}


